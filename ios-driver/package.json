{
  "name": "ios-driver",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell that ships the driver screens as a native iOS app",
  "scripts": {
    "prepare:web": "node scripts/copy-web.mjs",
    "sync": "npm run prepare:web && cap sync ios",
    "open": "cap open ios",
    "add:ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.8",
    "@capacitor/ios": "^7.6.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  }
}
