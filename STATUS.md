# สถานะโปรเจ็ค

อัปเดตล่าสุด: 15 สิงหาคม 2569

ไฟล์นี้ตอบคำถามเดียว — **ทำถึงไหนแล้ว และต้องทำอะไรต่อ** อ่านก่อนเริ่มงานทุกครั้ง

---

## ภาพรวม

TMS มีสองระบบที่ต่อกัน

**1. ตัวดึงข้อมูล (`extractor/`)** — เสร็จแล้ว ใช้งานได้จริง
ดึง Picking List และ Actual Shipment จาก TMS บริษัท (`pdi.vespiario.net`) แล้ว export เป็น Excel

**2. ระบบจ่ายงานคนขับ (`server/` + `web/`)** — ใช้งานอยู่บน LAN ของออฟฟิศ
กำลังย้ายขึ้นออนไลน์ (GitHub Pages + Supabase) ซึ่งเป็นงานที่ค้างอยู่ตอนนี้

```
TMS บริษัท ──► extractor ──► ระบบจ่ายงาน ──► คนขับ
             (เสร็จ)        (ใช้อยู่)      (ใช้อยู่)
                                │
                                └──► ย้ายขึ้นคลาวด์ (กำลังทำ)
```

---

## เสร็จแล้ว

### ตัวดึงข้อมูล `extractor/`

- ดึง Picking List พร้อมรายการ item
- ดึง Actual Shipment 23 คอลัมน์ + แตกเป็นแถวละ item (คอลัมน์ item อยู่ต่อจาก `Unit`)
- export Excel `.xlsx` จริง เขียนเอง ไม่มี dependency
- มีสองแบบ: เว็บที่รันผ่าน Node (`tms-extractor/`) และไฟล์เดียวยัดผ่าน console (`tms-extractor-standalone.html`)
- `TMS Extractor.bat` ติดตั้ง Node ให้อัตโนมัติถ้าเครื่องยังไม่มี

**กับดักที่เคยเสียเวลาไปแล้ว อย่าทำซ้ำ** — อ่าน `extractor/tms-extractor/README.md`
สรุปสั้น ๆ: ลำดับ element ใน `.xlsx` ต้องเป็น `sheetFormatPr → cols → sheetData → autoFilter → mergeCells`
สลับเมื่อไหร่ Excel ทิ้งทั้ง sheet แล้วฟ้อง `Load error. Line 1, column 0` ทั้งที่ XML ถูกทุกตัวอักษร

### ระบบจ่ายงาน `server/` + `web/`

ทำงานครบแล้ว 14 โมดูล มีเทส 79 ตัว ผ่านหมด รันบน LAN ผ่าน HTTPS (จำเป็น เพราะกล้องคนขับต้องการ secure context)

### ตัวเชื่อมสำหรับขึ้นคลาวด์ (เขียนเสร็จ ยังไม่ได้ใช้งานจริง)

| ไฟล์ | ทำอะไร |
|---|---|
| `supabase/migrations/0001_schema.sql` | 17 ตาราง แปลงจาก SQLite เป็น Postgres |
| `supabase/migrations/0002_permissions.sql` | ย้าย `ROLE_PRESET` จากโค้ดมาเป็นข้อมูล + `app.has_perm()` |
| `supabase/migrations/0003_rls.sql` | RLS policy ทุกตาราง |
| `supabase/migrations/0004_driver_api.sql` | view + ฟังก์ชันฝั่งคนขับ |
| `supabase/migrations/0005_storage.sql` | ถังเก็บรูป POD แบบ private |
| `supabase/all.sql` | ทั้งห้าไฟล์รวมกัน สำหรับวางใน SQL Editor ทีเดียว |
| `supabase/functions/tms-sync/` | Edge Function ดึง Actual Shipment จาก TMS อัตโนมัติ |
| `web/src/api/supabase.ts` | ตัวเชื่อม + แปลง error Postgres เป็นข้อความไทย |
| `web/src/api/auth.ts` | ล็อกอิน โปรไฟล์ สิทธิ์ |
| `web/src/api/myjobs.ts` | ฝั่งคนขับ ผ่าน view + RPC เท่านั้น |
| `web/src/api/storage.ts` | อัปโหลด/อ่านรูป POD ผ่าน signed URL |
| `web/src/types/database.ts` | ชนิดข้อมูลของทุกตาราง |
| `.github/workflows/deploy-pages.yml` | build แล้ว deploy ขึ้น GitHub Pages |

ทั้งหมดผ่าน `npm run typecheck` และเทสเดิมยังเขียวครบ

---

## ค้างอยู่ตรงนี้

### รัน migration แล้ว — เสร็จ 15 ส.ค. 2569

โปรเจ็ค: org `vppwtrs`, ref `ypkwavdtkhgvhuacwgla`, region Seoul (`ap-northeast-2`), Free plan
รัน 0001–0005 ทีละไฟล์ผ่าน SQL Editor ผ่านหมดทุกไฟล์

ผลตรวจ:

| ตรวจอะไร | ผล |
|---|---|
| ตาราง `public` | 15 ตาราง + view `my_trips` / `my_orders` (= 17 object ตามที่ตั้งใจ) |
| RLS | เปิดครบ 15/15 ไม่มีตารางไหนตกหล่น |
| policy | 41 ตัว |
| คอลัมน์เงินใน view คนขับ | ไม่มี (ค้นด้วย `fee|cost|price|amount` ได้ null) |
| `permissions` / `role_permissions` | 30 / 63 แถว |
| bucket `pod-photos` | `public = false` |

**เจอช่องหนึ่งระหว่างตรวจ แก้แล้ว** — `revoke all ... from anon` ใน 0003 รันก่อนที่ view
ใน 0004 จะเกิด แล้ว Supabase แจก grant ให้ `anon` กับ object ใหม่ใน `public` อัตโนมัติ
`anon` เลยถือสิทธิ์เต็มบน `my_trips` / `my_orders` ซึ่งเป็น view แบบ `security_invoker = off`
คือ view ที่ข้าม RLS ได้ตามออกแบบ ยังไม่รั่วจริงเพราะ view กรองด้วย `app.current_driver_id()`
ที่ anon ได้ null และ view มี join จึงเขียนไม่ได้ แต่ผิดกติกาข้อ 1 ของ 0003 เอง
เพิ่ม `revoke all on public.my_trips, public.my_orders from anon;` ลงใน 0004 แล้ว
ตอนนี้ `anon` เหลือ 0 สิทธิ์ใน schema `public`

### ผู้ใช้ + auth_id — เสร็จ 15 ส.ค. 2569

สร้างบัญชี Supabase Auth ไว้สองใบ (รหัสผ่านผู้ใช้พิมพ์เอง auto-confirm ทั้งคู่)
แล้ว insert `public.users` ผูก `auth_id` ให้:

| username | role | อีเมลที่ใช้ล็อกอิน | หมายเหตุ |
|---|---|---|---|
| `admin` | admin | `admin@tms.local` | บัญชีใช้งานจริง |
| `driver01` | driver | `driver01@tms.local` | บัญชีทดสอบ RLS ผูกกับ `drivers.id = 1` |

โดเมน `tms.local` ไม่ใช่โดเมนจริง — ใช้ได้เพราะ auto-confirm ข้ามการส่งอีเมล
ถ้าจะเปิดให้รีเซ็ตรหัสผ่านทางอีเมลได้ ต้องเปลี่ยนไปใช้อีเมลจริง

**ตรวจ RLS ข้อสุดท้ายผ่านแล้ว** — จำลองสองเที่ยว (ของ driver01 กับของคนขับอื่น)
ใน transaction แล้ว rollback ทิ้ง ยิงด้วยสิทธิ์ของ driver01 ได้ผล:

| ตาราง | driver01 เห็น |
|---|---|
| `orders` `trips` `customers` `vehicles` `pod` | 0 แถวทุกตาราง |
| `users` `drivers` | 1 แถว (ของตัวเอง) |
| `my_trips` `my_orders` | 1 / 1 เฉพาะเที่ยวตัวเอง ไม่เห็นของคนขับอื่น |

### แก้ `tms-sync` ให้ตรงกับ API จริง — เสร็จ 15 ส.ค. 2569

เทียบ `supabase/functions/tms-sync/index.ts` กับสิ่งที่ `extractor/` เจอของจริงมาก่อน แล้วเจอ 4 จุดที่ผิด
สองจุดแรกอันตรายเป็นพิเศษเพราะ **ไม่ทำให้ sync ล้ม** — มันขึ้นว่าสำเร็จแล้วเขียนข้อมูลว่างลงฐานเงียบ ๆ

| ผิดยังไง | แก้เป็น |
|---|---|
| อ่าน `r.details` — แต่ `/v1/reports/actualshipment` ส่ง 23 ฟิลด์แบน ไม่มี item เลย | ดึงจาก `/v1/pickinglistheaders/{wh}/search` แบบไล่หน้า แล้ว join ด้วยเลข PL (`fetchDetails()`) |
| ไม่มีตรรกะตัดหาง `-C-04` ตอน join | ยก `plKeyVariants()` มาจาก extractor ตรง ๆ |
| เก็บแต่ `qty` | เก็บ `item_split_qty` ด้วย + บันทึก `qty_source` ว่ายอดไหนตรงกับ `unit` |
| `plan_delivery_date` อ่าน `r.planDeliveryDate` ซึ่งเป็นชื่อ**พารามิเตอร์ตอนค้นหา** ไม่ใช่ฟิลด์ใน response | เปลี่ยนคอลัมน์เป็น `trip_date` อ่านจาก `orderDate` |

migration `0006_tms_shipments_fix.sql` รันขึ้น Supabase แล้ว คอลัมน์ปัจจุบัน:
`id, picking_list_no, trip_no_tms, trip_date, dealer_name, branch, unit, item_no, item_name,
item_qty, raw, synced_at, order_id, item_split_qty, qty_source`

**secret เพิ่มอีกตัว** — `TMS_WAREHOUSE_CODE` (เช่น `KM23-CW-01`)
สอง endpoint อ้างคลังคนละแบบ: รายงานใช้ GUID (`TMS_WAREHOUSE_ID`) ส่วน picking list header ใช้รหัส
ตั้งไม่ครบ = ฟังก์ชันโยน error ตั้งแต่บรรทัดแรก

ตอนนี้ response ของ sync คืน `missingItems` กับ `qtyMismatch` มาด้วย — สองตัวนี้คือสัญญาณว่า join พัง
หรือ API เปลี่ยนโครง ถ้า `missingItems` เท่ากับจำนวนแถวทั้งหมดคือ join ไม่ติดเลย

### ตัวเชื่อมโมดูลออฟฟิศ — เสร็จ 15 ส.ค. 2569

`0007_office_api.sql` + 6 ไฟล์ใน `web/src/api/` (customers, vehicles, orders, trips, quotes, reports)
`vehicles.ts` มีพนักงานขับอยู่ด้วย เพราะหน้าจัดเที่ยวเรียกสองอย่างนี้พร้อมกันเสมอ

เส้นแบ่งว่าอะไรเป็น RPC อะไรยิงตารางตรง — **ไม่ได้แบ่งด้วยสิทธิ์ แต่แบ่งด้วยจำนวนตารางที่แตะ**

| ยิงตารางตรง | RPC ใน 0007 |
|---|---|
| ลูกค้า รถ คนขับ ใบเสนอราคา (CRUD) | `create_trip` `add_orders_to_trip` `remove_order_from_trip` |
| อ่านรายการ/รายละเอียดทุกอย่าง | `dispatch_start_trip` `dispatch_complete_trip` `dispatch_cancel_trip` |
| ค่าน้ำมัน/ทางด่วนของเที่ยว | `convert_quote` |

เหตุผล: ตอนอยู่บน Express ตัว service ห่อ `db.transaction()` ให้ทั้งก้อน
พอมาเป็น PostgREST หน้าจอยิงทีละ request — สร้างเที่ยวผ่าน แล้วเน็ตหลุดก่อน update รถ
จะได้เที่ยวที่มีออเดอร์แต่รถยังว่าง สถานะที่ไม่มีทางเกิดตอนอยู่บน Express

เลขเอกสาร (`ORD-2026-0001`) เติมด้วย trigger ตอน insert — หน้าจอไม่ต้องคิดเลขเอง
ใช้ปี ค.ศ. เหมือน `generateDocNo()` เดิม

ทดสอบวงเต็มใน transaction แล้ว rollback: สร้างเที่ยว → ออกวิ่ง → ปิดเที่ยว → แปลงใบเสนอราคา
ได้ `TRP-2026-0001 / completed`, ออเดอร์สองใบเป็น `delivered`, ใบเสนอราคาเป็น `accepted`
พร้อมออเดอร์ใหม่ `ORD-2026-0003`, รถกับคนขับคืนเป็น `available`

**ยังไม่ได้ทำ** — `reports.ts` รวมยอดฝั่งเบราว์เซอร์ ไม่ใช่ SQL (ของเดิมเป็น query รวมยอดสิบกว่าตัว)
ระดับข้อมูลตอนนี้เร็วพอ แต่ถ้าช่วงที่เลือกเกิน ~5,000 แถวต้องย้ายไปเป็น RPC ที่คืน json ก้อนเดียว
อย่าฝืนเพิ่มการรวมยอดในไฟล์นั้นต่อ

### ยืนยันกับ TMS ตัวจริง + สะพานเข้าระบบ — 15 ส.ค. 2569

เข้าไปดู API จริงผ่าน session ที่ผู้ใช้ล็อกอินไว้ (อ่านอย่างเดียว) ได้ข้อสรุปที่เดาไม่ได้จากโค้ด:

| เรื่อง | ของจริง |
|---|---|
| ราก API | `https://pdi.vespiario.net/tms-api/api` — **ไม่ใช่แค่โดเมน** ใส่ผิด 404 ทุก endpoint |
| response ของ actualshipment | array แบน 24 คีย์ **ไม่มี `details[]`** และ **ไม่มี `planDeliveryDate`** |
| PL header | ค้นด้วย `keyword` ได้ตรงใบ เจอเลขเต็มรวมหาง `-C-0n` |
| ขนาดคลัง | PL header 14,915 ใบ = 30 หน้า × 500 |

**คำถาม `ตรวจยอด` ที่ค้างมานาน — ตอบแล้ว** วัด 40 PL จริง (รวมใบมีหาง 3 ใบ):
`unit` ตรงกับผลรวม `qty` **40/40** ไม่มีใบไหนต้องใช้ `splitQty` เลย
ยังเก็บ `item_split_qty` ไว้เหมือนเดิม เพราะ 40 ใบจากคลังเดียวช่วงเดียวไม่ใช่ข้อสรุปถาวร
`qty_source` ทำหน้าที่เป็นตัวเฝ้า — วันไหนมันเริ่มเป็น `split` หรือ null แปลว่าสมมติฐานเปลี่ยน

`tms-sync` เปลี่ยนจาก **ไล่หน้าทั้งคลัง** เป็น **ค้นทีละใบด้วย keyword** (ทีละ 5 ใบพร้อมกัน)
ของเดิมคือดึง 14,915 ใบพร้อม details ทุกคืนเพื่อหา PL ไม่กี่สิบใบ

**สะพานเข้าระบบ (`0008_tms_bridge.sql`)** ออกแบบจากตัวเลขจริง 9 วันของคลัง KM23-CW-01:
209 แถว = 209 PL ไม่ซ้ำ (1 PL = 1 ออเดอร์), 51 เที่ยว, 73 ร้าน, ~23 PL ต่อวัน

- `tms_dealer_map` — คนจับคู่ `dealerCode` กับลูกค้าครั้งเดียวต่อร้าน แล้วจำไว้
  ไม่เดาจากชื่อ เพราะชื่อร้านใน TMS กับชื่อลูกค้าเราไม่เหมือนกัน เดาผิด = ออเดอร์ไปผิดลูกค้าแบบเงียบ
  `customer_id is null` (ยังไม่ตัดสินใจ) ต่างจาก `ignored` (ตัดสินใจแล้วว่าไม่เอา) — อย่ายุบรวมกัน
- `preview_tms_import(date)` — ดูก่อนว่าวันนั้นมีกี่ใบ ร้านไหนยังไม่จับคู่ ทะเบียนไหนไม่รู้จัก ไม่แตะข้อมูลจริง
- `import_tms_shipments(date)` — สร้างออเดอร์เฉพาะใบที่ร้านจับคู่แล้ว ใบที่เหลือ**ข้าม ไม่ล้มทั้งวัน**
  เรียกซ้ำวันเดิมไม่สร้างซ้ำ (ใบที่มี `order_id` แล้วถูกกรองออก)

**ตั้งใจไม่เดา 3 อย่าง** — `fee` ตั้ง 0 (TMS มีแต่ `actualCost` = ต้นทุนที่เราจ่าย ไม่ใช่ราคาที่เก็บลูกค้า),
`weight_kg` ตั้ง 0 (`unit` คือจำนวนคัน ห้ามเอามาใส่ช่องกิโล), และ**ไม่สร้าง `trips` ให้อัตโนมัติ**
เพราะ `tripNo` ของ TMS คือ "ใครวิ่งไปแล้ว" (อดีต) ส่วน `trips` ของเราคือแผนที่คนจัดรถกดยืนยัน
ออเดอร์ที่นำเข้าจึงเป็น `pending` รอจัดเที่ยวตามปกติ เก็บ `tripNo` เดิมไว้ใน `notes` ให้ตามรอยได้

### เปลี่ยนวิธีดึงข้อมูล: คนกดส่ง แทน Edge Function ตั้งเวลา — 15 ส.ค. 2569

เจ้าของโปรเจ็คบอกตรง ๆ ว่าดูแลฝั่งคลาวด์ไม่ไหว และสิ่งที่ต้องการจริง ๆ มีอย่างเดียว
คือ**หน้าจ่ายงานให้คนขับ** จึงตัดสโคปเหลือสองท่อ: TMS → Supabase และ Supabase → หน้าเว็บ

แผนเดิม (`tms-sync` ยิงเองตอนตี 1) ต้องเก็บ user/password ของ TMS บริษัทไว้ใน Supabase Secrets
= รหัสบริษัทไปนอนบนคลาวด์ต่างประเทศ ต้องขอ IT ก่อน และถ้ารหัสหมดอายุก็เงียบไปจนกว่าจะมีคนสังเกต

**แบบใหม่**: คนออฟฟิศเปิด `extractor/` บนเครื่องตัวเอง ล็อกอิน TMS ด้วยรหัสตัวเอง (รหัสอยู่ในหัวคน
ไม่มีในไฟล์ ไม่มีบนคลาวด์) ดึงข้อมูล แล้วกดปุ่ม **ส่งขึ้นระบบ** ใหม่ในหน้านั้น
เขียนลง Supabase ด้วย**บัญชีพนักงานคนนั้นเอง** ไม่ใช่ `service_role` → ไม่มี secret ให้เก็บเลยสักตัว

ทำไมตัวดึงขึ้นคลาวด์ไม่ได้: เบราว์เซอร์ยิงหา `pdi.vespiario.net` จากโดเมนอื่นไม่ได้ (CORS)
`server.js` ของ extractor ทำ proxy ให้อยู่แล้ว ตัวดึงจึงต้องอยู่บนเครื่องเสมอ ส่วน "ที่เก็บ" ย้ายขึ้นคลาวด์ได้

ของที่เพิ่ม:

| ไฟล์ | ทำอะไร |
|---|---|
| `supabase/migrations/0009_tms_push.sql` | `tms_sync_log` + `push_tms_shipments(jsonb)` + `tms_sync_status(date)` — **รันแล้ว** |
| `extractor/tms-extractor/public/config.js` | URL + anon key ของ Supabase |
| `extractor/tms-extractor/public/push.js` | ล็อกอิน Supabase + แปลงแถว + ส่งทีละ 400 แถว |

จุดที่ต้องระวังในโค้ดนั้น:
- `item_no` ว่างเก็บเป็น `''` ไม่ใช่ `null` — Postgres ถือว่า `null` ไม่ชนกับ `null` ถ้าปล่อย null
  แถว "PL ที่ไม่มี item" จะงอกใหม่ทุกครั้งที่กดส่ง
- ต้อง `distinct on` ในฟังก์ชัน ไม่งั้นเจอ *ON CONFLICT DO UPDATE command cannot affect row a second time*
  เมื่อก้อนเดียวมี (PL, item) ซ้ำกันเอง — เกิดจริงกับ PL ที่ถูกแบ่งส่งหลายเที่ยว
- `order_id` ไม่ถูกแตะตอน upsert ใบที่นำเข้าเป็นออเดอร์แล้วกดส่งซ้ำก็ไม่หลุด
- หน้าจอแสดงวันที่เป็น `dd/mm/yyyy` (`fmtD`) ต้องแปลงกลับเป็น ISO ก่อนส่ง

**โหมดอัตโนมัติ** (ติ๊กช่องในหน้าต่างส่งขึ้นระบบ) ล็อกอิน TMS เสร็จปุ๊บ ดึงเมื่อวานแล้วส่งเองเลย
จากนั้นวนซ้ำทุกชั่วโมงตราบที่เปิดหน้าค้างไว้ — เกาะ `boot()` ผ่าน `onBooted()` ที่เรียกแบบมีเงื่อนไข
(`typeof onBooted === 'function'`) เพื่อให้ก๊อป extractor ไปใช้เดี่ยว ๆ โดยไม่มี `push.js` ได้อยู่
session ของ Supabase จึงย้ายไปอยู่ localStorage + ต่ออายุด้วย refresh_token เอง
ไม่งั้นคนต้องพิมพ์รหัสสองระบบทุกเช้า แล้วสุดท้ายจะเลิกกด

วนทุกชั่วโมง ไม่ใช่ทุก 5 นาที — TMS เป็นระบบที่คนทั้งบริษัทใช้ ดึงถี่เกินจำเป็นคือไปกินทรัพยากรเขา
และ `autoBusy` กันรอบซ้อนกันเมื่อรอบก่อนยังไม่จบ

ข้อเสียที่รับไว้: เช้าไหนไม่มีใครเปิดเครื่อง ข้อมูลก็ไม่มา — จึงมี `tms_sync_log` ให้หน้าออฟฟิศ
ขึ้นแถบเตือนว่า "วันนี้ยังไม่มีใครดึง" และใครก็กดได้ ไม่ผูกกับคนเดียว

`supabase/functions/tms-sync/` **ยังอยู่แต่ไม่ใช้แล้ว** อย่าเพิ่งลบ — ถ้าวันหนึ่ง IT อนุมัติ
ให้เก็บ service account บนคลาวด์ ค่อยเปิดใช้เป็นตัวสำรองอัตโนมัติได้ทันที

### ตัวตนออฟฟิศ = บัญชี TMS บริษัท — 15 ส.ค. 2569

เจ้าของโปรเจ็คตัดสินใจสามข้อ แล้วสถาปัตยกรรมเปลี่ยนตาม:

1. **พนักงานออฟฟิศล็อกอินด้วยบัญชี TMS บริษัท** — ล็อกอินผ่าน = เป็นพนักงานจริง
   ไม่ต้องสร้างบัญชีอีกชุดให้คนจำสองรหัส แต่ admin ยังเป็นคนกำหนดสิทธิ์
2. **คนขับใช้บัญชี Supabase ล้วน ไม่เกี่ยวกับ TMS เลย** — เหมือนเดิมทุกอย่าง
3. **`extractor/` ที่เป็นโปรแกรมแยกใช้ไม่ได้จริง** ต้องยุบเข้าแอปเดียว

ข้อ 3 ติดที่ CORS: เบราว์เซอร์ยิงหา `pdi.vespiario.net` จากโดเมนอื่นไม่ได้ ต้องมีตัวกลาง
เดิมตัวกลางคือ `server.js` บนเครื่อง ซึ่งบังคับให้ตัวดึงเป็นโปรแกรมแยกตลอดกาล
**ย้ายตัวกลางขึ้น Edge Function** แอปจึงเหลือตัวเดียว เปิดจากที่ไหนก็ได้

บริษัทอนุมัติแล้วว่าทำได้ตราบใดที่**ไม่แก้ข้อมูลภายในของเขา** — ซึ่งเราอ่านอย่างเดียวมาตลอด

`supabase/functions/tms-gateway/index.ts` (ใหม่) ทำสองหน้าที่:

| route | ทำอะไร |
|---|---|
| `/auth` | ส่ง user/pass ไปถาม TMS จริง ผ่านแล้วออก session ของ Supabase ให้ |
| `/call` | ส่งต่อคำขอ**อ่าน**ข้อมูลไปยัง TMS (แก้ CORS) |

ข้อจำกัดที่ใส่ไว้ตั้งใจ **ห้ามถอด**:
- `ALLOW` เป็นรายการ regex ของ path ที่ยิงได้ทั้งหมด **ไม่มี endpoint ที่เขียนกลับเข้า TMS**
- ไม่รับ URL จาก client — ยิงได้ที่เดียวคือ `TMS_BASE_URL` ถ้ารับ ตัวนี้จะกลายเป็น open proxy
- **ห้าม `console.log(body)`** log ของ Edge Function อ่านย้อนหลังได้ และ body มีรหัสผ่านอยู่
- รหัสฝั่ง Supabase สุ่มใหม่ทุกครั้งที่ล็อกอิน ไม่มีใครรู้รวมทั้งเจ้าตัว ใช้แค่ในฟังก์ชันแล้วทิ้ง
- ฟังก์ชันนี้**ไม่เก็บรหัสของบริษัทไว้เลยสักตัว** ต่างจากแผน `tms-sync` เดิม
  คนที่ยึด secret ไปได้ก็ยังล็อกอิน TMS ไม่ได้

`0010_tms_identity.sql` (**รันแล้ว**) — บัญชีที่เกิดจากการล็อกอิน TMS ครั้งแรกเป็น
`is_active = false` ไม่มีสิทธิ์อะไรเลย จนกว่า admin กด `approve_user()`
ที่ใช้ `is_active` เดิมแทนคอลัมน์ `status` ใหม่ เพราะ `app.current_user_id()` กรองอยู่แล้ว
บัญชีรออนุมัติจึงไม่มีตัวตนในสายตา RLS ทุก policy **โดยไม่ต้องแก้ policy สักตัว**
ผลข้างเคียงคือคนรออนุมัติอ่านแม้แต่แถวตัวเองไม่ได้ → `my_account()` อ่านจาก `auth.uid()` ตรง ๆ

**ข้อมูลจำลองอยู่ที่เดียวคือ `server/src/db/seed.ts`** (ลูกค้า 15 ราย + ออเดอร์ + รถ + คนขับ
+ ใบเสนอราคา) ลง SQLite เท่านั้น — **Supabase ไม่มีข้อมูลปลอมสักแถว** มีแค่ `settings` 3 แถว
พอสลับหน้าจอมาอ่าน Supabase ข้อมูลจำลองหายไปเองโดยไม่ต้องลบอะไร

### หน้าเข้าสู่ระบบสองทาง — 15 ส.ค. 2569

เขียนแล้ว **ยังไม่ถูกเรียกจากหน้าจอ** ตามกติกาเดิม (ระบบที่ออฟฟิศใช้อยู่ต้องยังรันได้)
`App.tsx` ยังใช้ `AuthContext` + `Login.tsx` ตัวเก่าที่คุยกับ Express อยู่

| ไฟล์ | |
|---|---|
| `api/tmsAuth.ts` | `signInWithTms()` + `tmsCall()` ผ่าน gateway |
| `api/users.ts` | `listPending` / `approveUser` / `revokeUser` |
| `context/CloudAuthContext.tsx` | ตัวแทน `AuthContext` — มีสองทางเข้า + สถานะรออนุมัติ |
| `pages/CloudLogin.tsx` | สองแท็บ: พนักงานออฟฟิศ / พนักงานขับรถ |

ตัดสินใจที่ควรรู้:
- **แท็บสองอัน ไม่ใช่ช่องเดียวแล้วเดา** เดาผิดแล้วผู้ใช้ไม่มีทางรู้ว่าผิดตรงไหน
  พนักงานออฟฟิศที่พิมพ์รหัส TMS ลงช่องคนขับจะเจอ "รหัสผิด" ทั้งที่รหัสถูก
- **`PendingApprovalError` แยกจาก "ล็อกอินไม่ผ่าน"** และ**ไม่ signOut** —
  คง session ไว้ให้ถามซ้ำได้หลัง admin อนุมัติ โดยผู้ใช้ไม่ต้องพิมพ์รหัสใหม่
- token ของ TMS อยู่ใน **sessionStorage** ต่างจาก session ของเราที่อยู่ localStorage
  เพราะเป็น token ของระบบบริษัท ปิดแท็บแล้วควรหาย ไม่ควรค้างข้ามวัน
- `loadProfile()` ถาม `my_account()` เมื่อหาแถวตัวเองไม่เจอ เพื่อแยก "รออนุมัติ"
  ออกจาก "ยังไม่ผูก auth_id" — สองอย่างนี้ผู้ใช้ต้องทำคนละอย่างกัน

**ไม่มีการสร้างบัญชีพนักงานออฟฟิศจากหน้าจอแล้ว** บัญชีเกิดเองตอนล็อกอิน TMS ครั้งแรก
ผลพลอยได้: ไม่มีใครต้องตั้งรหัสผ่านให้คนอื่น (วิธีที่รหัสรั่วบ่อยที่สุด — พิมพ์ในแชท ส่งไลน์)
และคนที่ลาออกแล้วถูกปิดบัญชีที่ TMS ก็เข้าระบบเราไม่ได้ทันทีโดยไม่มีใครต้องจำไปกดปิดซ้ำ

### ยุบ extractor เข้าแอป — 15 ส.ค. 2569

`api/tmsPull.ts` + `pages/TmsPull.tsx` (ยังไม่ถูกเรียกจากหน้าจอเช่นกัน)
ย้ายตรรกะมาจาก `extractor/tms-extractor/public/app.js` ทั้งหมด

ค่าคงที่ทุกตัวมาจากการวัดข้อมูลจริง **อย่าแก้โดยไม่วัดใหม่**:
- `CHUNK = 5` — ยิงหา TMS พร้อมกันทีละ 5 ใบ ไม่ใช่ทั้งหมดพร้อมกัน
  นี่คือระบบที่คนทั้งบริษัทใช้อยู่ ยิงรัวเป็นร้อย request คือไปกินทรัพยากรเขา
- **ค้นทีละใบด้วย keyword ไม่ไล่หน้าทั้งคลัง** — คลังนี้มี PL รวม ~15,000 ใบ (30 หน้า × 500)
  ไล่หน้าคือดึงมาหมดเพื่อใช้ไม่กี่สิบใบ ค้นทีละใบใช้ ~23 request ต่อวัน
- `PUSH_CHUNK = 400` — ส่งทีเดียวหมดคือเน็ตสะดุดแล้วเสียทั้งก้อน
- ค่าเริ่มต้นเป็น**เมื่อวาน** ไม่ใช่วันนี้ รอบข้อมูล TMS ปิดหลังเที่ยงคืน

`qtySource` ยังเก็บไว้เป็นตัวเฝ้าระวัง แม้วัดแล้วว่า `unit` ตรงกับผลรวม `qty` ทั้ง 40/40 ใบ —
ถ้าวันหนึ่งค่านี้เริ่มว่างบ่อย ๆ แปลว่า TMS เปลี่ยนความหมายของ `unit`

`extractor/` **ยังไม่ลบ** ลบได้ต่อเมื่อหน้านี้ถูกใช้งานจริงแล้วเท่านั้น

### build คู่ขนาน — 15 ส.ค. 2569

**ไม่สับสวิตช์ทีเดียว** ออฟฟิศทำงานกับระบบเดิมทุกวัน สับพลาดคืองานหยุดทั้งออฟฟิศกลางสัปดาห์
ราคาที่จ่ายคือมีโค้ดสองชุดอยู่พักหนึ่ง ซึ่งถูกกว่าออฟฟิศหยุดหนึ่งวัน

```bash
npm run build -w web         # ระบบเดิม — Express + SQLite บน LAN (ค่าเริ่มต้น)
npm run build:cloud -w web   # ระบบใหม่ — Supabase ขึ้น GitHub Pages
```

`main.tsx` เลือกด้วย `import.meta.env.MODE` ที่ Vite มีให้อยู่แล้ว **ไม่ตั้งตัวแปรใหม่** —
ตัวแปรใหม่ต้องมีไฟล์ `.env` ซึ่ง `.gitignore` กันไว้ CI จึงจะไม่เห็นแล้ว build ผิดตัวโดยไม่มีใครรู้

`import()` แบบ dynamic ทั้งสองฝั่ง เพราะ `api/supabase.ts` โยน error ตั้งแต่ตอน import
ถ้าไม่มี env ของ Supabase — import ตรง ๆ จะทำให้ build ฝั่ง LAN พังทั้งที่ไม่ได้ใช้ Supabase เลย

ยืนยันด้วยการ build จริงทั้งคู่: ฝั่ง cloud ไม่มี chunk ของหน้าฝั่ง Express ติดไปเลยสักไฟล์

`0011_my_orders_fields.sql` (**รันแล้ว**) เติม `distance_km` + `customer_address` ลง `my_orders`
`customer_address` ไม่ใช่ของประดับ — เป็นปุ่มเปิดแผนที่นำทาง ถ้าไม่มี คนขับต้องพิมพ์ที่อยู่เอง
ขณะขับรถ ซึ่งแย่กว่าไม่มีปุ่มเสียอีก ทั้งสองคอลัมน์ไม่ใช่ตัวเลขเงิน
**ต้อง `drop` ก่อน `create`** เพราะ `create or replace view` เปลี่ยนจำนวนคอลัมน์ไม่ได้
และเมื่อ drop แล้ว grant หายไปด้วย ต้อง `revoke ... from anon` ใหม่ทุกครั้ง (ตรวจแล้ว: anon โดน 42501)

`api/myjobs.ts` มี `listMyJobs()` แปลงสอง view กลับเป็นรูป `MyJob` ที่หน้าจอเดิมใช้ —
ตั้งใจไม่ไปรื้อ `MyJobs.tsx`/`JobFocus.tsx` เพราะหน้าคนขับผ่านการปรับจนลงตัวกับการใช้จริงในรถแล้ว
รื้อเพื่อเปลี่ยนแหล่งข้อมูลคือเอาของที่ใช้ได้ดีไปเสี่ยงโดยผู้ใช้ไม่ได้อะไรเพิ่ม

POD: อัปรูปขึ้น Storage **ก่อน** บันทึก — ลำดับกลับกันจะได้หลักฐานที่อ้างถึงรูปที่ไม่มีอยู่
ส่วนแบบนี้แย่สุดคือมีรูปกำพร้าค้างในถัง ซึ่งไม่ทำใครเดือดร้อน

`AppCloud.tsx` มีแค่ 4 หน้า (login / my-jobs / tms-pull / users) — หน้าออฟฟิศที่เหลือ
ตัวเชื่อมใน `api/` เสร็จหมดแล้วแต่หน้าจอยังไม่แปลง **ตั้งใจไม่ใส่ route** เพราะ route
ที่พาไปหน้าที่ยังไม่แปลง = ผู้ใช้เจอหน้าพังโดยไม่รู้ว่าเพราะอะไร

### เว็บขึ้นออนไลน์แล้ว — 15 ส.ค. 2569

**https://vppwtrs.github.io/tms/** ใช้งานได้จริง

repo `vppwtrs/tms` เปลี่ยนเป็น public แล้ว ตรวจประวัติ git ทั้งหมดก่อนเปิด: JWT ในประวัติมีตัวเดียว
decode ได้ `role: anon`, ไม่มี `service_role` / `sbp_` / `ghp_` / private key หลุดสักคอมมิต,
`.env` ไม่เคยถูก track, CSV ใน `server/data/csv/` เป็นข้อมูลปลอมล้วน (`contact1@example.com`)

Pages ใช้กับ repo private ไม่ได้ในแพลนฟรี — นี่คือเหตุผลเดียวที่ต้องเปิด public ไม่ใช่เพราะอยากเปิด

ผลตรวจของจริงหลัง deploy:

| ตรวจ | ผล |
|---|---|
| asset ทุกไฟล์ + console | 200 ครบ ไม่มี error |
| ล็อกอินคนขับ (ยิงรหัสผิดตั้งใจ) | ได้ข้อความไทยจาก Supabase Auth จริง |
| `tms-gateway/auth` (ยิงรหัสผิดตั้งใจ) | `401 {"error":"เข้าสู่ระบบ TMS ไม่สำเร็จ"}` |

**คำถาม WAF ที่ค้างมานาน — ตอบแล้ว** มีคนล็อกอินด้วยบัญชี TMS บริษัทผ่านจริงเมื่อ 17:11
(บัญชี `laksiya.t_vespiario.net@tms.invalid` เกิดจากการเทส ไม่ใช่ของเจ้าของโปรเจ็ค)
แปลว่า **Supabase Edge ที่ Seoul ยิงถึง `pdi.vespiario.net` ได้ ไม่โดน WAF บล็อก**
และรหัส TMS บริษัทใช้ยืนยันตัวตนผ่าน API ได้จริง

**SPA fallback คืน status 404** — `/tms/my-jobs` เสิร์ฟ `404.html` (ซึ่งคือ index.html)
ผู้ใช้เห็นหน้าถูกต้องแต่ status เป็น 404 เป็นข้อจำกัดของ Pages แก้ไม่ได้ กระทบแค่ SEO

**CI ล้มมาตั้งแต่คอมมิตแรกทั้ง 6 รอบ** — ไม่เกี่ยวกับโค้ด `ci.yml` ตรึง Node 20 ไว้
แต่ jsdom ที่เทส a11y ใช้ ดึง undici รุ่นที่เรียก `webidl.util.markAsUncloneable` ซึ่งไม่มีใน Node 20
worker ของ vitest เลยตายตั้งแต่ยังไม่ทันรันเทส (เครื่อง dev รัน 24 จึงไม่เจอ) แก้เป็น 22 แล้ว

**บัญชี `admin@tms.local` ใช้ไม่ได้ — `Last signed in: –` ไม่เคยถูกใช้เลย**
แดชบอร์ด Supabase มีให้แค่ "Send password recovery" ซึ่งส่งไปที่โดเมนที่ไม่มีจริง = ตายทาง
ทางแก้คือลบใน Auth แล้วสร้างใหม่พร้อม **Auto Confirm** แล้ว**ต้อง**ผูก `auth_id` ใหม่ใน `public.users`
เพราะ UID เปลี่ยน — `auth_id` เป็น `on delete set null` แถวใน `public.users` จึงไม่หายตามไป
ลืมขั้นผูกกลับ = ล็อกอินผ่านแต่ไม่เห็นข้อมูลอะไรเลย อาการเหมือนรหัสผิดจนหาสาเหตุไม่เจอ

### กฎการเลือกทางล็อกอิน — ตกลงแล้ว 15 ส.ค. 2569

หน้าล็อกอินเหลือ **ช่องเดียว** ไม่มีแท็บให้เลือก ระบบตัดสินเองจาก**โดเมน**:

| กรอกอะไร | ยิงถามใคร | ครั้งแรก |
|---|---|---|
| `xxx@vespiario.net` | TMS บริษัท ผ่าน `tms-gateway` | เกิดบัญชีรอ admin อนุมัติ |
| ไม่มี `@` | TMS บริษัท | เหมือนกัน |
| `@tms.local` / โดเมนอื่น | Supabase Auth | ไม่ต้อง — ออฟฟิศสร้างให้อยู่แล้ว |

**ห้ามเปลี่ยนกลับเป็น "มี `@` = บัญชีระบบนี้"** — ชื่อผู้ใช้ TMS ที่นี่เป็นรูปแบบ
`Laksiya.T@vespiario.net` คือมี `@` ในตัว กฎแบบนั้นทำให้พนักงานออฟฟิศทุกคน
ถูกส่งไปถาม Supabase แล้วเจอ "รหัสไม่ถูกต้อง" ทั้งที่รหัสถูก (เคยเกิดมาแล้ว)

**ตัดสินก่อนส่งคำขอเสมอ ห้ามลองยิงทีละทาง** — ลอง Supabase ก่อนแล้วค่อยไป TMS
แปลว่ารหัสของบริษัทถูกส่งขึ้นคลาวด์ทุกครั้งที่พนักงานออฟฟิศล็อกอิน ผิดกติกาข้อหลัก

โดเมน `vespiario.net` hardcode อยู่ใน `CloudLogin.tsx` ถ้าบริษัทมีโดเมนที่สอง
คนกลุ่มนั้นจะเจออาการเดียวกัน ต้องเพิ่มเป็นรายการ

การอนุมัติบังคับที่ฐานข้อมูล ไม่ใช่หน้าจอ — บัญชีใหม่เป็น `is_active = false`
และ `app.current_user_id()` กรองคนที่ไม่ active ออก ทุก policy จึงมองไม่เห็นเขา
ยิง API ตรงข้ามหน้าเว็บก็ไม่ได้อะไร

### Picking List เป็นแหล่งเดียว + เฝ้าสถานะทุก 5 นาที — 17 ส.ค. 2569

เข้าไปดูหน้า Picking List ของจริง แล้วพบว่าเส้นที่ใช้มาก่อนหน้านี้ผิดตั้งแต่การเลือกแหล่ง
`POST /v1/pickinglistheaders/{wh}/search` ก้อนเดียวส่งครบกว่าทุกอย่างที่ระบบนี้ต้องใช้:

| ของที่ PL มี แต่ `actualshipment` ไม่มี | ใช้ทำอะไร |
|---|---|
| `status` New → AssignTrip → OnTruck → Completed | รู้ว่างานถึงไหน |
| `tripStatus` Confirm → OnDelivery → Completed | รู้ว่าเที่ยวถึงไหน |
| `planDeliveryDate` | วันที่ใช้วางแผนจริง — `trip_date` ของใบ New เป็น null ตลอด |
| `shipTo*` ชื่อ/ที่อยู่/จังหวัด/รหัสไปรษณีย์ | ปุ่มนำทางของคนขับ + สร้างลูกค้าใหม่ได้ทันที |
| `details[]` ติดมากับ header | ไม่ต้องยิงหา item อีก ~23 request ต่อวัน |

**ตัดเส้น `actualshipment` ทิ้งทั้งเส้น** (`pullShipments` / `fetchDetails` / `plKeyVariants` /
`warehouseGuid` หายไปด้วย เพราะมีไว้รับใช้เส้นนั้นตัวเดียว)

`0012_tms_pl_status.sql` — คอลัมน์ใหม่ 16 ตัว + เขียน `push_tms_shipments` ใหม่ + `tms_board()`
+ `create_customer_from_dealer()` และ `preview`/`import` เปลี่ยนไปตัดสินด้วย `plan_delivery_date`

จุดที่ต้องรู้:
- **`plan_delivery_date` กับ `trip_date` คนละตัว ห้ามยุบรวม** ตัวแรกคือวันที่วางแผนส่ง
  ตัวหลังคือ `orderDate` = วันของเที่ยวที่ TMS จับใบเข้าไป ว่างได้ถ้ายังไม่จัดเที่ยว
  ของเดิมกรองด้วย `trip_date` ทำให้ใบสถานะ New (ที่ต้องวางแผนมากที่สุด) หลุดหมด
  ข้อมูลเก่าจากเส้น actual ไม่มีวันวางแผน จึง**ไม่เติมย้อนหลัง** ทุก query ใช้ `coalesce` ของสองตัว
- **`row_hash` ตัดสินว่าจะเขียนหรือไม่** — `where tms_shipments.row_hash is distinct from excluded.row_hash`
  รอบ 5 นาทีส่วนใหญ่ไม่มีอะไรเปลี่ยน ถ้าเขียนทับทุกรอบคือเขียนฐานเปล่า ๆ วันละ ~280 ครั้ง
  คิด hash ฝั่ง SQL ไม่ใช่ให้ client ส่งมา ไม่งั้นใครส่ง hash มั่วก็สั่งข้ามการอัปเดตได้
  `raw` ไม่อยู่ในสูตร เพราะ TMS ส่ง GUID/textDisplay มาด้วย เอามาคิดแล้วทุกแถวจะ "เปลี่ยน" ตลอด
- `push` คืน `inserted / updated / unchanged` แยกกัน และ **ลง `tms_sync_log` เฉพาะรอบที่มีของเปลี่ยน**
  แจ้ง "สำเร็จ" ทุก 5 นาทีคือฝึกให้คนเลิกอ่านข้อความของระบบ
- **เก็บ Completed ไว้ในตาราง แต่ไม่นำเข้าเป็นออเดอร์** กรองตอนดึงคือใบที่ส่งจบหายจากตารางเงียบ ๆ
  ตัวกรองอยู่ที่ `import_tms_shipments` ไม่ใช่ตอนดึง
- `import` เพิ่ม `and coalesce(plan_delivery_date, trip_date) = p_date` ตอน set `order_id`
  ของเดิมอัปเดตทุกแถวที่เลข PL ตรง ถ้า TMS เลื่อนวันส่ง ใบของวันอื่นจะถูกตีว่านำเข้าแล้วโดยไม่มีออเดอร์จริง
- ปลายทางของออเดอร์เป็น **ที่อยู่จริง** (`ship_to_address` + จังหวัด) ไม่ใช่ชื่อร้าน ชื่อร้านอยู่ที่ลูกค้าแล้ว

**รอบเฝ้าสถานะ ≠ โหลดทั้งก้อน** `pullRecent()` ดึงแค่ 2 หน้าแรก (1,000 ใบล่าสุด) ย้อนหลัง 3 วัน
ถึงล่วงหน้า 14 วัน = 2 request ต่อรอบ · 24 req/ชม. ไล่ทั้งคลัง ~15,000 ใบทุก 5 นาทีคือ
ไปกินทรัพยากร TMS ที่คนทั้งบริษัทใช้อยู่ โดยได้ผลเท่ากัน ปุ่ม "ดึงย้อนหลัง" ที่คนกดเองยังไล่ได้ 60 หน้า
`running` เป็น ref กันรอบซ้อน (state อ่านค่าเก่าตอน timer ยิง)

**ทางตัน "ไม่มีลูกค้าให้เลือก" แก้แล้ว** ปุ่ม "สร้างลูกค้าใหม่จากร้านนี้" ในหน้านำเข้า
เรียก `create_customer_from_dealer()` ที่สร้างลูกค้า + จับคู่ในฟังก์ชันเดียว
แยกสองคำสั่งแล้วเน็ตหลุดกลางทาง = ลูกค้าที่ไม่ผูกกับร้านไหนลอยอยู่ แล้วคนกดสร้างใหม่ซ้ำ
**ยังไม่ใช่การเดา** — คนกดต่อร้าน ระบบไม่ match ชื่อให้เอง

หน้าจอทั้งสองหน้าเลิกเดาวันจากปฏิทินเครื่อง ถาม `tms_board()` ว่าวันล่าสุดที่มีงานคือวันไหน
ตั้งเป็น "เมื่อวาน" แล้วข้อมูลมีถึงสองวันก่อน คนจะเจอ "ยังไม่มีข้อมูล" ทั้งที่มีงานค้างอยู่

ตรวจแล้ว: `npm run typecheck` ผ่านสอง workspace · `npm test -w web` 11/11 · `build:cloud` ผ่าน

### เที่ยวของ TMS เป็นแหล่งที่สอง — 17 ส.ค. 2569

หน้าที่ต้องดูไม่ใช่ Picking List อย่างเดียว แต่เป็น **Trip** ด้วย
`POST /v1/tripheaders/{warehouseGuid}/search` (อ้างคลังด้วย **GUID** ต่างจาก PL ที่ใช้รหัส)

วัดของจริงก่อนออกแบบ:

| คลัง | เที่ยวทั้งหมด | carrier | ของกองรถเรา |
|---|---|---|---|
| `8b2c7a7a` KM23-CW-02 | 172 | Fleet Owner ล้วน | 172/172 |
| `5af1fd5f` KM23-CW-01 | 4,200 | Fleet Owner (Scooter) + ATM · NGCH · KTS · TOLL · VES | 66 จาก 200 ที่สแกน |
| `f73c1803` KM12-MQ-01 | 1,940 | ATM · NGCH · KTS · Naphat · VES | **0 — ไม่มีเลย** |

**ตัวเลขที่เปลี่ยนสโคปทั้งงาน: กองรถเราวิ่งวันละ 2–6 เที่ยว** ไม่ใช่ 23 ใบจากทุกเจ้า

สถานะที่ TMS ใช้จริง — เที่ยว `2 Confirm → 3 Handling → 4 OnDelivery → 5 Completed` · `6 Cancelled`
ใบในเที่ยวของเรา `2 AssignTrip → 3 OnTruck → 4 Completed`

**สองแหล่งเสริมกัน ไม่ใช่แทนกัน อย่าตัดอันใดอันหนึ่ง**
`tms_shipments` (PL) = "มีของต้องส่ง" — ใบสถานะ New ยังไม่มีเที่ยว **ไม่โผล่ในหน้า Trip เลย**
`tms_trips` (Trip) = "ใครวิ่ง ถึงไหนแล้ว" — มีทะเบียน คนขับ ต้นทุน และสถานะที่ขยับทั้งวัน

`0013_tms_trips.sql`:
- `tms_carriers` — **กรองด้วย carrier ไม่ใช่ด้วยคลัง** เจ้าที่ไม่รู้จักถือว่าไม่ใช่ของเรา
  ไม่ใช่เดาว่าใช่ (เที่ยวหายมีคนทัก งานผิดเจ้าไม่มีใครทัก) เป็นตารางไม่ใช่ค่าคงที่ในโค้ด
  เพราะวันที่บริษัทตั้งกองรถชุดที่สามจะต้องแก้ทั้ง client + function พร้อมกัน ซึ่งจะลืมข้างหนึ่ง
- `tms_trips` คีย์เป็น **GUID ของ TMS ไม่ใช่ `trip_no`** เลขเที่ยวเป็นรูป `yyyymmddnnn` ซ้ำข้ามคลังได้
- `tms_shipments.tms_trip_id` ผูกใบเข้าเที่ยว · `push_tms_trips` เขียนแต่ตัวเที่ยวแล้ว **ผูก** ใบด้วยเลข PL
  เนื้อใบยังมีทางเข้าทางเดียวคือ `push_tms_shipments` (สองฟังก์ชันเขียนตารางเดียวกัน = ตรรกะเพี้ยนแล้วหาไม่เจอ)
- `tms_driver_map` / `tms_vehicle_map` หลักเดียวกับ `tms_dealer_map`
  ชื่อคนขับมาแบบ `"เอกชัย บุญอินทร์ (เอก)"` — วงเล็บคือชื่อเล่น **เก็บทั้งก้อน อย่าตัด** ตัดแล้วจับคู่ผิดคนที่ชื่อซ้ำ
- `create_driver_from_tms` / `create_vehicle_from_tms` — คนขับที่เกิดจากที่นี่ **ไม่มี `user_id`**
  จึงเข้าแอปไม่ได้จนกว่าจะมีคนสร้างบัญชีให้ ตั้งใจให้เป็นแบบนั้น
- `import_tms_trip` สร้างออเดอร์ + เที่ยว + ผูก + จองรถ/คนขับ ในทรานแซกชันเดียว
  จองรถเฉพาะเที่ยวที่ยังไม่จบ — นำเข้าย้อนหลังแบบ Completed ไม่ควรทำให้รถที่วิ่งงานวันนี้กลายเป็นไม่ว่าง
- `tms_board` เขียนใหม่: **นับเที่ยวเป็นตัวหลัก** ยอดใบกับยอดคันเป็นตัวรอง คนจัดรถคิดเป็นเที่ยว

**กลับหลักการที่ 0008 เขียนไว้** ว่าไม่สร้าง `trips` อัตโนมัติเพราะ "`tripNo` ของ TMS คืออดีต"
เหตุผลนั้นตกไปเมื่อเห็นของจริง: `Confirm` คือแผนที่ยังไม่ออกรถ `Handling` คือกำลังโหลดของ
ทั้งสองเป็นอนาคตกับปัจจุบัน (เที่ยว `20260817001` ของวันนี้เป็น `Confirm`)
ให้คนจัดรถพิมพ์เที่ยวเดิมซ้ำในระบบเราคือทำงานสองรอบเพื่อผลลัพธ์เดียวกัน
แต่ยังให้กดยืนยัน**ทีละเที่ยว** ไม่ auto

**ทะเบียน/คนขับที่ยังไม่จับคู่กันการนำเข้า** ต่างจากร้านที่ยังไม่จับคู่ซึ่งข้ามใบนั้นได้ —
เที่ยวที่ไม่มีรถกับคนขับไม่ใช่เที่ยว มันคือแถวเปล่า

**TMS ยกเลิกเที่ยวที่เรานำเข้าไปแล้ว (`status_id = 6`) ระบบไม่ยกเลิกให้เอง**
รถอาจวิ่งออกไปแล้ว — ขึ้นเตือนบนสุดของหน้าพร้อม `reason` ให้คนตัดสิน

ฝั่งเว็บ: `pullTrips` / `pullRecentTrips` / `pushTrips` + `warehouseGuid()` (เอากลับมาจากที่ลบตอน 0012)
หน้าใหม่ `CloudTmsTrips.tsx` (`/tms-trips`) · หน้าดึงข้อมูลดึงสองแหล่งในรอบเดียว (ใบก่อนเที่ยว เพราะผูกด้วยเลข PL)

`testTimeout` ของ vitest ขยับ 5s → 15s: เทส dashboard รันเดี่ยว 2.1 วินาที แต่รันขนาน 5.04 วินาที
= ล้มที่เส้น default พอดี เส้นนั้นวัดว่าเครื่องว่างแค่ไหน ไม่ได้วัดโค้ด

ตรวจแล้ว: `typecheck` ผ่านสอง workspace · `npm test -w web` 11/11 · build ทั้งสองโหมดผ่าน
**ยังไม่ได้รัน `0013` บน Supabase**

### เที่ยวไปทั้งก้อน · รถไม่ล็อกกับคน · สถานะไหลตาม TMS — 17 ส.ค. 2569

เจ้าของงานชี้ว่า 0013 เข้าใจผิดสามข้อ แก้ด้วย `0014_trip_order_whole.sql`

**1. หน่วยที่ส่งให้คนขับคือ "เที่ยว" ทั้งก้อน**
ของเดิมข้ามใบที่ร้านยังไม่จับคู่ → คนขับได้เที่ยวที่**ขาดจุดส่ง**ไปเงียบ ๆ
เขาขับไปถึงหน้าร้านแล้วในระบบไม่มีงานนั้น ซึ่งแย่กว่าออเดอร์ที่ยังไม่รู้ว่าเป็นลูกค้ารายไหน
ตอนนี้สร้างออเดอร์ **ทุกใบในเที่ยว** ใบที่ร้านยังไม่จับคู่ก็เป็นออเดอร์ที่ `customer_id` เป็น null
(คอลัมน์นี้ยอม null ตั้งแต่ 0001) และ **ชื่อร้านถูกใส่นำหน้าที่อยู่ในช่อง `destination`**
ไม่ใส่ = คนขับเห็นแต่ที่อยู่เปล่า ๆ ไม่รู้ว่าไปส่งใคร
`api/myjobs.ts` ดึงชื่อจาก `destination` มาโชว์แทน "ไม่ระบุลูกค้า" เมื่อยังไม่มีลูกค้า
`link_tms_orders_to_customers()` เติมลูกค้าย้อนหลังหลังจับคู่ร้าน — ต้องมี ไม่งั้นทางแก้เดียว
คือลบออเดอร์แล้วนำเข้าใหม่ ซึ่งพา POD ที่คนขับเก็บไว้หายไปด้วย

**2. รถล็อกกับคนไม่ได้** กองรถมีแต่ 4W 5 คัน คนขับคนเดิมไม่ได้ใช้คันเดิมทุกวัน
**ห้ามมีที่ไหนในระบบผูก "คนขับคนนี้คู่กับรถคันนี้"** รถผูกที่ระดับ **เที่ยว** เท่านั้น
ซึ่งเป็นความจริงเฉพาะวันนั้น
ทะเบียนไม่ใช่เรื่องที่ต้องให้คนตัดสิน — TMS บอกมาแล้วว่าเที่ยวนี้ใช้คันไหน
`import_tms_trip` จึงสร้างรถให้เองถ้ายังไม่มี **เลิกบล็อกการนำเข้าเพราะทะเบียน**
ชื่อคนขับยังบล็อก เพราะต้องผูกกับบัญชีผู้ใช้จริงถึงจะเห็นงานตัวเอง (RLS แขวนกับ `drivers.user_id`)
เดาผิด = งานไปโผล่ในมือคนที่ไม่ได้วิ่ง แล้วคนที่วิ่งจริงมองไม่เห็นงานตัวเอง

**3. สถานะที่เปลี่ยนต้องไหลถึงคนขับ** `sync_tms_trip_status()` อัปเดต `trips`/`orders`
ของเที่ยวที่นำเข้าแล้วตามสถานะ TMS — เรียกท้าย push อัตโนมัติผ่าน `push_tms_trips_and_sync()`
ไม่ให้หน้าจอสั่ง เพราะวันไหนมีคนเขียนหน้าใหม่แล้วลืมเรียก สถานะจะค้างโดยไม่มีอะไรฟ้อง
**ไหลไปข้างหน้าทางเดียว** (`app.trip_rank` / `app.order_rank`) — คนขับกดปิดงาน + เก็บ POD แล้ว
แต่ที่ TMS ยังขึ้น OnDelivery ถ้าถอยสถานะกลับ งานที่เสร็จแล้วจะเด้งกลับมาเป็นงานค้าง
และหลักฐานจะขัดกับสถานะงาน · ไม่แตะออเดอร์ที่ `delivered`/`cancelled` แล้วเลย

`typecheck` ผ่าน · เทส 11/11 · build ผ่าน · **ยังไม่ได้รัน `0014`**

### ขั้นถัดไป

| # | งาน | ทำไมต้องอยู่ลำดับนี้ |
|---|---|---|
| 1 | ~~รัน migration + ตรวจ RLS~~ | **เสร็จแล้ว** (ถึง `0009`) |
| 2 | ~~สร้างผู้ใช้ + ผูก `auth_id`~~ | **เสร็จแล้ว** (2 บัญชี — ที่เหลือตามมาตอนย้ายข้อมูล) |
| 3 | ~~ย้ายข้อมูลจาก SQLite เข้า Postgres~~ | **เสร็จแล้ว** — `tms.db` เป็นข้อมูลทดสอบล้วน ย้ายแค่ `settings` 3 แถว |
| 4 | ~~เขียนตัวเชื่อมโมดูลออฟฟิศ~~ | **เสร็จแล้ว** — 6 ไฟล์ใน `web/src/api/` + migration `0007` |
| 5 | ~~ท่อ TMS → Supabase~~ | **เสร็จแล้ว** — ปุ่มส่งขึ้นระบบใน extractor (`0009`) แทน Edge Function |
| 6 | ~~สะพานตัวตน + ตัวกลาง CORS~~ | **เขียนเสร็จ** — `tms-gateway` + `0010` (รันแล้ว) เหลือ deploy |
| 7 | ~~deploy `tms-gateway` + ตั้ง `TMS_BASE_URL`~~ | **เสร็จแล้ว** — ยืนยันด้วยการล็อกอินจริงผ่าน TMS |
| 8 | ~~ย้ายตรรกะดึงข้อมูลจาก `extractor/` เข้า `web/src/api/`~~ | **เสร็จแล้ว** — เหลือเส้น PL เดียว (`0012`) |
| 9 | ~~หน้าจับคู่ร้าน + ปุ่มนำเข้า~~ | **เสร็จแล้ว** — พร้อมปุ่มสร้างลูกค้าจากร้าน |
| 9.1 | **รัน `0012` บน Supabase** | เขียนเสร็จแล้ว ยังไม่ได้รัน — หน้าเว็บใหม่จะ error จนกว่าจะรัน |
| 10 | หน้าอนุมัติพนักงาน | `approve_user` / `revoke_user` มีแล้ว ขาดแค่หน้าจอ |
| 11 | สลับ MyJobs มาใช้ Supabase | หน้าเดียวที่เจ้าของโปรเจ็คต้องการจริง ๆ |
| 12 | ลบ `seed.ts` + `extractor/` ทิ้ง | ทำหลังข้อ 8 และ 11 ผ่านแล้วเท่านั้น ไม่ใช่ก่อน |
| 13 | ~~`git init` + push ขึ้น GitHub~~ | **เสร็จแล้ว** — ตรวจประวัติทั้งหมดก่อนเปิด public |
| 14 | ~~ตั้ง repo variable + เปิด Pages~~ | **เสร็จแล้ว** — https://vppwtrs.github.io/tms/ |
| 15 | ~~กู้บัญชี admin (ลบ + สร้างใหม่ + ผูก `auth_id`)~~ | **เสร็จแล้ว** |
| 16 | ~~ทดสอบหน้า tms-pull ดึงข้อมูลจริง~~ | **เสร็จแล้ว 15 ส.ค. 2569** — ดึงได้ 142 แถว 2 วัน จากคลัง KM23-CW-01 |
| 17 | แปลงหน้าออฟฟิศที่เหลือ | เหลือ CustomerDetail, Quotes, Reports, Dashboard, Settings, Data |

### ดึงข้อมูล TMS — สิ่งที่รู้เพิ่มตอนทำจริง

`/personal/warehouses` คือคลังที่ผูกกับ **ตัวบุคคล** ไม่ใช่คลังที่สิทธิ์ของบัญชีมองเห็น
บัญชีจริงหลายคนได้ค่าว่างจาก endpoint นี้ทั้งที่เปิดดูคลังได้ในหน้า TMS
`extractor/` กลบปัญหานี้ด้วยการฝัง `KM23-CW-01` เป็นค่าเริ่มต้นไว้ในโค้ด ซึ่งเป็นเหตุผล
ที่คลังที่สอง (`KM23-CW-02`) ไม่เคยโผล่ในตัวเดิมเลย — แอปถอยไปถาม `/v1/warehouses/search` แทน

`/v1/warehouses/search` รับ `pageNumber` / `pageSize` **ไม่ใช่** `page` / `keyword`
ส่งชื่อผิดแล้วมันตอบผลว่างเงียบ ๆ ไม่ error

### หน้าที่แปลงขึ้นคลาวด์แล้ว

ลูกค้า · ดึงข้อมูล TMS · นำเข้าเป็นออเดอร์ · ออเดอร์ · แผนงานขนส่ง · รถยนต์ · พนักงานขับ ·
ผู้ใช้และสิทธิ์ · งานของฉัน

**ยังไม่มีบนคลาวด์และตั้งใจไม่ใส่**: ปุ่มพิมพ์ใบนำส่ง (`/orders/:id/bol`) กับหน้าต่าง POD
(`/pod/...`) ทั้งคู่เป็น endpoint ของ Express ที่ยังไม่มีตัวแทนฝั่งคลาวด์

**ห้ามสลับข้อ 13-14 มาก่อนข้อ 1** — deploy เว็บ public ก่อนที่ RLS จะถูกต้อง
คือเปิดประตูก่อนติดกลอน

---

## เรื่องความปลอดภัยที่ตัดสินใจไปแล้ว อย่ารื้อ

**`service_role` key ห้ามอยู่ใน repo หรือ frontend เด็ดขาด** มันข้าม RLS ทั้งหมด
ที่ของมันคือ Edge Function secret เท่านั้น ส่วน `anon` key เปิดเผยได้ นั่นคือการออกแบบ ไม่ใช่ความผิดพลาด

**API ซ่อนไม่ได้และไม่ต้องซ่อน** URL, endpoint, โค้ด frontend ทั้งหมดเปิดให้เห็นได้
สิ่งที่ต้องกันคือสิทธิ์ ไม่ใช่ความรู้ว่า endpoint ชื่ออะไร
เกณฑ์ตัดสิน: ถ้าคนขับเปิด DevTools แล้วรู้ทุกอย่างเกี่ยวกับ API ระบบต้องยังปลอดภัยอยู่

**คนขับห้ามเห็นตัวเลขเงิน** เดิมกันด้วยการที่ repository ไม่ SELECT คอลัมน์นั้นมาตั้งแต่ต้น
ตอนนี้กันด้วย view `my_trips` / `my_orders` ที่ไม่มีคอลัมน์เงินอยู่ในนั้นเลย
**RLS กันคอลัมน์ไม่ได้ กันได้แค่แถว** — นี่คือเหตุผลที่คนขับต้องอ่านผ่าน view ไม่ใช่ตารางจริง

**รหัส TMS บริษัทห้ามอยู่บนคลาวด์ และห้ามพิมพ์ลงหน้าเว็บสาธารณะ**
เดิมวางแผนเก็บเป็น secret ให้ `tms-sync` ใช้ — เลิกแล้ว เพราะเป็นรหัสของบริษัทไปนอนอยู่กับบุคคลที่สาม
ตอนนี้พนักงานพิมพ์รหัสลง extractor ที่รันบน `localhost` ของเครื่องตัวเองเท่านั้น
**อย่าย้ายหน้าล็อกอิน TMS ไปอยู่บน `github.io` เด็ดขาด** — เป็นการฝึกให้คนโดนฟิชชิ่ง
ถ้าวันหนึ่งจะกลับไปทำอัตโนมัติ ต้องขอ IT เปิด service account สิทธิ์อ่านอย่างเดียวก่อน ไม่ใช่ใช้รหัสของคน

**การกระทำของคนขับต้องเป็นฟังก์ชัน ไม่ใช่ update ตรง** `complete_trip()` เหมาออเดอร์ที่เหลือ
เป็น delivered ให้หมด ถ้าปล่อยให้หน้าจอยิง update เอง คนขับปิดงานที่ยังไม่ได้ส่งได้ทั้งเที่ยว
ปุ่มที่ disable ไว้กันคนที่ยิง API ตรงไม่ได้

---

## กับดักทางเทคนิคที่เจอมาแล้ว

**`interface` ใช้เป็น type ของตารางไม่ได้** postgrest-js ต้องการ `Record<string, unknown>`
ซึ่ง `interface` ไม่เข้าเงื่อนไข ต้องเป็น `type` เท่านั้น
เผลอเขียน `interface` เมื่อไหร่ ทั้งไฟล์กลายเป็น `never` แบบหาสาเหตุไม่เจอ

**plpgsql ไม่ลัดวงจร `and` ใน IF** — `if tg_table_name = 'orders' and new.order_no ...`
พังด้วย 42703 ทันทีที่ trigger ยิงจากตารางที่ไม่มีคอลัมน์นั้น เพราะทั้งเงื่อนไขถูกส่งไปให้ SQL
ประเมินเป็นนิพจน์เดียว ต้องเขียน IF ซ้อนแทน (คนละ statement = คอมไพล์แยกกัน)

**`.maybeSingle()` ทำ generic inference พังเป็น `never`** เช็ค `error` เองตรงนั้น อย่าฝืนใช้ type helper

**ห้าม `force row level security` บนตาราง `users`** `app.current_user_id()` เป็น security definer
ที่อ่าน `users` — เปิด force เมื่อไหร่ policy จะเรียกฟังก์ชัน ฟังก์ชันอ่าน users แล้วโดน policy เดิมอีก วนไม่จบ

**เครื่องนี้รัน `npm test -w server` ไม่ผ่าน** — `better-sqlite3` prebuilt ที่ติดตั้งมาไม่ตรง ABI
ของ Node 24.16 (`NODE_MODULE_VERSION 137`) เทสฝั่ง server ล้มหมด 60 ตัวตั้งแต่ `new Database()`
ไม่เกี่ยวกับโค้ด — เทสฝั่ง web ผ่านครบ 11 ตัว และ `npm run typecheck` ผ่านทั้งสอง workspace
แก้ด้วย `npm rebuild better-sqlite3` หรือถอยไป Node 22

**`authenticated` ไม่มี `usage` บน schema `app`** เรียก `app.has_perm()` ตรง ๆ จาก client
จะได้ `permission denied for schema app` — ไม่กระทบระบบเพราะ policy ประเมินด้วยสิทธิ์เจ้าของตาราง
(ทดสอบแล้วทำงานถูก) และ client เรียกแต่ฟังก์ชันใน `public`
ถ้าวันหนึ่งอยากให้หน้าจอถาม `has_perm` ตรง ๆ ต้อง `grant usage on schema app to authenticated`

**`.bat` ต้องเป็น ASCII ล้วน** ข้อความไทยอยู่ใน `.ps1` แทน และ `.ps1` ต้องเป็น UTF-8 with BOM

---

## ที่ยังไม่รู้ ต้องหาคำตอบ

**คอลัมน์ `ตรวจยอด` ใน Actual Shipment** — ยอด `unit` กับ `Item Qty` ไม่ตรงกันในบางใบ
เพราะ PL ที่ถูกแบ่งส่งหลายเที่ยว (เลขลงท้าย `-C-04`) ตอนนี้แสดงทั้ง `Item Qty` และ `Split Qty`
พร้อมคอลัมน์ตรวจยอดไว้ก่อน **ต้องดูข้อมูลจริงว่าส่วนใหญ่ตรงกับตัวไหน** แล้วค่อยยุบเหลือคอลัมน์เดียว

ตอนนี้ `tms-sync` บันทึกคำตอบสะสมไว้ให้แล้วในคอลัมน์ `qty_source` — sync จริงสักพักแล้ว query ตอบได้เลย:

```sql
select qty_source, count(*) from public.tms_shipments group by qty_source;
```

(เคยมี `extractor/_check.xlsx` เป็นไฟล์ทดสอบตัวเขียน xlsx ซึ่งตอบคำถามนี้ไม่ได้ — ลบทิ้งแล้ว
พร้อมโฟลเดอร์ `extractor/Other/` ที่เป็นบันทึกสำรวจ API รุ่นแรก ๆ ข้อสรุปที่ใช้จริงอยู่ในไฟล์นี้หมดแล้ว)

**Supabase free tier พอมั้ย** — Storage 1GB รูป POD ใบละ ~300KB คิดเป็นราว 3,000 ใบ
วิ่งวันละ 30 จุดก็ประมาณ 3 เดือนเต็ม ต้องมีแผนลบของเก่าหรือจ่ายเพิ่ม

**WAF ของ TMS** — `pdi.vespiario.net` เป็น IP สาธารณะของ AWS สิงคโปร์ คลาวด์น่าจะยิงถึง
แต่ยังไม่ได้ทดสอบว่ามีการจำกัด IP หรือเปล่า ถ้ามี ต้องกลับไปใช้เครื่องในออฟฟิศเป็นตัวดึง

**นโยบายบริษัท** — การเก็บรหัสระบบบริษัทไว้บนคลาวด์ของบุคคลที่สาม
หลายที่มีนโยบายห้าม ควรถาม IT ก่อนทำ ไม่ใช่ทำแล้วค่อยบอก

---

## คำสั่งที่ใช้บ่อย

รันระบบเดิม (server + web):

```bash
npm run dev
```

ตรวจก่อนส่งงาน — ต้องผ่านทั้งคู่:

```bash
npm run typecheck && npm test
```

สร้าง `all.sql` ใหม่หลังแก้ migration:

```bash
cd supabase && cat migrations/*.sql > all.sql
```

รัน extractor แบบเว็บ:

```bash
node extractor/tms-extractor/server.js
```

build extractor แบบไฟล์เดียว:

```bash
node extractor/tms-extractor-standalone/build.js
```
