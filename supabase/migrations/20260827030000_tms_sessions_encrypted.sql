-- token ในตารางนี้ถูกเข้ารหัสแล้ว — บันทึกไว้ในฐานเองเพื่อไม่ให้ใครเผลออ่านตรง ๆ
--
-- รูปแบบ: "iv.ciphertext" ฐาน base64 ทั้งสองส่วน AES-GCM 256 บิต
-- คีย์ไม่ได้อยู่ในฐาน อยู่ที่ secret TMS_TOKEN_KEY ของ Edge Function tms-gateway
-- ตั้งใจให้คนที่ dump ฐานออกไปได้ทั้งก้อน ยังใช้อะไรไม่ได้ถ้าไม่ได้คีย์จากอีกระบบ
--
-- เปลี่ยนคีย์เมื่อไหร่ แถวเก่าทั้งหมดถอดไม่ออกและถูกลบทิ้งตอนมีคนเรียกใช้
-- ผลคือทุกคนล็อกอินใหม่หนึ่งครั้ง ไม่มีข้อมูลงานสูญหาย — เปลี่ยนคีย์ได้อย่างสบายใจ

comment on column public.tms_sessions.token is
  'token ของ TMS ที่เข้ารหัสแล้ว รูปแบบ iv.ciphertext (base64, AES-GCM) '
  'คีย์อยู่ที่ secret TMS_TOKEN_KEY ของ Edge Function ไม่ได้อยู่ในฐาน';
