# TMS Web — อัปเดตสถานะโปรเจกต์

อัปเดตล่าสุด: 18 สิงหาคม 2026

## สถานะปัจจุบัน

ระบบเว็บถูก deploy ผ่าน GitHub Pages และเชื่อมต่อ Supabase project สำหรับระบบของเราเองแล้ว โดยไม่เขียนข้อมูลกลับไปยัง TMS บริษัท

## งานที่ทำเสร็จแล้ว

- ระบบล็อกอินแยกบัญชีบริษัท TMS และบัญชีระบบของเรา
- บัญชีบริษัทใช้ TMS gateway สำหรับตรวจสอบตัวตนและดึงข้อมูลแบบอ่านอย่างเดียว
- ถ้าบัญชีบริษัทถูกสร้างแล้วแต่ยังไม่อนุมัติ ระบบจะแจ้งเตือน แล้วออกจาก session และกลับหน้า Login อัตโนมัติ
- แก้กรณี `auth_id` ใน `public.users` ชี้ไปยัง Auth ที่ถูกลบ ทำให้ล็อกอินบริษัทไม่สำเร็จ
- `tms-gateway` ตรวจ Auth ที่หายไปแล้วสร้างตัวเชื่อมใหม่ให้อัตโนมัติ
- เพิ่มการลบบัญชีถาวรจากหน้าเว็บ ลบทั้ง Supabase Auth และ `public.users` ในครั้งเดียว
- บัญชีที่ลบแล้วไม่ลบประวัติออเดอร์หรือเที่ยวงาน
- เพิ่มตัวเลือกบทบาทตอนสร้างผู้ใช้:
  - ผู้ดูแลระบบ
  - วางแผนงาน
  - ดูอย่างเดียว
  - พนักงานขับรถ
- เพิ่มปุ่มลบถาวรสำหรับรายการใน “บัญชีเก็บถาวร”
- แยกเลข Trip จริง, เลข PL และประเภทงานรถ/กล่องในข้อมูลออเดอร์
- เปิด RLS ให้ตาราง public ทั้งหมด และใช้นโยบายสำหรับผู้ใช้ที่ authenticated
- ซ่อน service role key และให้การสร้าง/ลบรหัสผ่านทำผ่าน Edge Function เท่านั้น

## Edge Functions ที่ใช้งาน

- `tms-gateway` — ตรวจสอบบัญชีบริษัทและ proxy เฉพาะ endpoint แบบอ่านข้อมูล
- `admin-users` — สร้างบัญชีคนขับและสุ่มรหัสผ่านใหม่
- `admin-delete-user` — ลบบัญชี Auth และข้อมูล profile ใน `public.users`

## การทำงานของข้อมูล

```text
ล็อกอิน TMS บริษัท
        ↓
Supabase Edge Function: tms-gateway
        ↓
ดึง Trip จากเมนู Trip ของ TMS แบบอ่านอย่างเดียว
        ↓
บันทึกข้อมูลที่จำเป็นลง Supabase ของเรา
        ↓
จัดออเดอร์ → จัดเที่ยว → มอบหมายคนขับ → POD
```

## ข้อควรระวัง

- ห้ามนำรหัสผ่านบริษัทหรือ service role key ใส่ใน frontend, GitHub หรือไฟล์ Markdown
- การลบบัญชีถาวรควรใช้ปุ่มในหน้าเว็บ ไม่ควรลบ Auth จาก Supabase Dashboard โดยตรง เพราะอาจเหลือ profile ค้างใน `public.users`
- การลบบัญชีจะไม่ลบประวัติธุรกิจ เพื่อไม่ให้รายงานและประวัติการส่งของเสียหาย
- ผู้ใช้บริษัทจะเห็นข้อมูลได้ก็ต่อเมื่อ admin อนุมัติบทบาทและสิทธิ์แล้ว

## ปัญหา permission ของ dispatcher

ถ้าผู้ใช้แสดงบทบาทเป็น “วางแผนงาน” แต่หน้าเว็บขึ้นว่าไม่มีสิทธิ์ แปลว่า role มีอยู่ใน `users` แล้ว แต่ยังไม่มี permission preset ใน `role_permissions` ให้รัน SQL นี้ใน Supabase SQL Editor:

```sql
insert into public.role_permissions (role, permission)
select
  'dispatcher',
  p.permission
from public.permissions p
where p.permission in (
  'customers.view',
  'customers.write',
  'orders.view',
  'orders.write',
  'dispatch.view',
  'dispatch.write',
  'drivers.view',
  'drivers.write',
  'vehicles.view',
  'vehicles.write',
  'myjobs.view'
)
on conflict do nothing;
```

หลังรัน SQL ให้ผู้ใช้ล็อกเอาต์และล็อกอินใหม่ ห้ามเพิ่ม `users.manage` ให้ dispatcher เพราะเป็นสิทธิ์ของ admin

## สถานะงานดึง Trip จาก TMS (17 สิงหาคม 2026)

แหล่งข้อมูลที่ถูกต้องคือเมนู **Trip** ของ TMS บริษัท:

- API ที่ใช้: `/v1/tripheaders/{warehouse-guid}/search`
- ไม่ใช้ Picking List เป็นแหล่งหลักของเที่ยว
- รับเฉพาะ `Fleet Owner` และ `Fleet Owner (Scooter)`
- สถานะ `Completed` เป็นข้อมูลที่นำเข้าได้ ไม่ใช่สาเหตุที่ข้อมูลหาย

สิ่งที่ตรวจพบจากหน้า TMS บริษัทจริง:

- หน้า Trip แสดงข้อมูลจริง 119 เที่ยว
- ตัวอย่างเที่ยว `20260817003` มี Carrier เป็น `Fleet Owner (Scooter)` และสถานะ `COMPLETED`
- ฝั่งเว็บของเราดึงจาก 2 คลังได้ 254 เที่ยวแล้ว แต่ก่อนแก้ระบบอ่านชื่อ Carrier จาก JSON ไม่ตรง จึงกรองทิ้งทั้งหมด

โค้ดที่แก้และ push แล้ว:

- `aacbd41` — แปลงชื่อคลังเต็ม เช่น `KM23-CW-01 - ...` เป็นรหัสคลัง และค้นต่อจาก warehouse search หาก personal warehouse ไม่ตรงกลุ่ม
- `b30e8ed` — อ่านชื่อ Carrier จากโครงสร้างข้อมูล Trip แบบทนต่อรูปแบบ JSON ของ TMS ก่อนกรอง Fleet Owner

หลัง GitHub Pages deploy ต้องรีเฟรชเว็บแบบแรง (`Ctrl+F5`) แล้วกด **ดึง Trip ทุกคลังในช่วงนี้** จากหน้า **ดึงข้อมูลจาก TMS**

### เกณฑ์ว่างานนี้ผ่าน

หลังการดึงรอบถัดไป ต้องตรวจให้ครบ:

1. หน้าดึงข้อมูลรายงานจำนวนเที่ยวของกองรถเรามากกว่า 0
2. `public.tms_trips` มีแถวเพิ่มขึ้น
3. หน้า **เที่ยวจาก TMS** มีวันที่ล่าสุดและแสดง Trip No จริง
4. หน้า **ออเดอร์** มีรายการที่นำเข้าจาก Trip ตาม workflow
5. กดดึงซ้ำแล้วไม่สร้าง Trip หรือ Order ซ้ำ

คำสั่งตรวจฐานข้อมูล (ใช้หลังกดดึงข้อมูล):

```powershell
npx supabase db query --linked "select count(*) as trips from public.tms_trips; select count(*) as orders from public.orders;"
```

หากผลยังเป็น 0 ให้เก็บข้อความสถานะจากหน้าดึงข้อมูลและตรวจ payload ของ Trip API ต่อ ไม่ควรกลับไปแก้ Picking List เพราะไม่ใช่ต้นทางของเที่ยว

## การตรวจสอบล่าสุด

- Production build: ผ่านหลัง commit `b30e8ed`
- โค้ดล่าสุดถูก push ขึ้น `main` แล้ว
- ยังรอทดสอบการดึงข้อมูลจริงหลัง GitHub Pages deploy และยืนยันจำนวนแถวใน Supabase

## งานถัดไปที่ควรทำ

1. รอ GitHub Pages deploy ของ commit `b30e8ed`
2. รีเฟรชเว็บและดึง Trip ทุกคลังในช่วงนี้
3. ตรวจ `tms_trips` และ `orders` ด้วยคำสั่งข้างต้น
4. ทดสอบดึงซ้ำเพื่อยืนยันว่าไม่เกิดข้อมูลซ้ำ
5. เมื่อมีข้อมูลแล้ว ตรวจการแสดงผลหน้าเที่ยวจาก TMS, ออเดอร์ และแผนงานขนส่งตามลำดับ

## สาเหตุที่ Trip ไม่เข้าระบบ และการแก้ (18 สิงหาคม 2026)

ต้นเหตุไม่ได้อยู่ที่ฝั่งเว็บ แต่อยู่ที่ฐานข้อมูล: `push_tms_trips` กรอง carrier ด้วย

```sql
join public.tms_carriers c on c.carrier_name = s.carrier_name where c.is_ours
```

แต่ตาราง `public.tms_carriers` ไม่เคยถูก seed จึงเป็น inner join กับตารางว่าง เที่ยวทุกใบถูกทิ้ง
และนับเป็น `skipped_carrier` โดยไม่ raise error หน้าเว็บจึงขึ้นว่า "ไม่มีอะไรเปลี่ยน" ทุกรอบ
`push_tms_trips` เป็นฟังก์ชันเดียวในฐานที่อ้างถึงตารางนี้ เส้น Picking List เลยไม่กระทบ
(`tms_shipments` มี 1,682 แถว ขณะที่ `tms_trips` และ `orders` เป็น 0)

commit `aacbd41` และ `b30e8ed` แก้การอ่าน carrier ฝั่ง client ถูกแล้ว แต่แก้ด่านนี้ไม่ได้

สิ่งที่ทำแล้ว:

- seed `Fleet Owner` และ `Fleet Owner (Scooter)` ลง `tms_carriers` บนฐานจริงเรียบร้อย
- เพิ่ม migration `supabase/migrations/20260818000000_trip_sync_fix.sql` — seed carrier,
  ให้ `push_tms_trips_and_sync` เขียน `tms_sync_log` ด้วย `source = 'trip'` ทุกครั้ง
  และเพิ่มตารางเข้า publication `supabase_realtime`
- หน้า **ดึงข้อมูลจาก TMS** ขึ้นคำเตือนชัดเจนเมื่อเที่ยวถูกกรองทิ้งทั้งชุดเพราะไม่รู้จัก carrier
  แทนที่จะกลืนเป็น "ไม่มีอะไรเปลี่ยน"

**migration ยังไม่ได้รัน** ต้องรัน `npx supabase db push --linked` ก่อน realtime และ log ฝั่ง trip จะทำงาน

## เรื่องเรียลไทม์

เดิมระบบไม่มี realtime เลย ไม่ใช่เพราะ Supabase ไม่เสถียร:

- publication `supabase_realtime` ไม่มีตารางอยู่เลยสักตาราง
- ฝั่ง client ไม่มีการเรียก `.channel()` ที่ไหน มีแค่ `onAuthStateChange`
- ที่เห็นว่าอัปเดตเองคือ `setInterval` 5 นาทีในหน้าดึงข้อมูล ซึ่งทำงานเฉพาะตอนเปิดแท็บนั้นค้างไว้

เพิ่มแล้ว: hook `web/src/hooks/useRealtime.ts` และต่อเข้าหน้า ออเดอร์, แผนงานขนส่ง,
เที่ยวจาก TMS และงานของฉัน โดย event เป็นแค่สัญญาณให้โหลดใหม่ (รวบ event ใน 400ms)
ไม่ได้เอาแถวดิบมาแปะทับ เพราะหน้าจอแสดงข้อมูลที่ join มาจากหลายตาราง

## หนี้ทางเทคนิคที่ยังค้าง

1. **การซิงก์ผูกกับเบราว์เซอร์** — ETL ทั้งชุดรันในแท็บของคนใช้ ปิดแท็บแล้วข้อมูลหยุดไหล
   ทางแก้คือย้ายเข้า Edge Function + `pg_cron` แต่ติดว่า `tms-gateway` ใช้ token ของ TMS
   ที่ได้จากรหัสผ่านของคนล็อกอินเท่านั้น ถ้าจะรันแบบ cron ต้องเก็บบัญชีบริการไว้ใน Supabase secret
   ซึ่งเป็นการตัดสินใจด้านความปลอดภัยที่ต้องเคาะก่อน ยังไม่ได้ทำ
2. **โค้ดสองสแตกซ้อนกัน** — `pages/*.tsx` ยิง Express ที่ `TMS/server` ส่วน `pages/Cloud*.tsx`
   ยิง Supabase GitHub Pages ไม่มี Express ครึ่งแรกจึงใช้งานจริงไม่ได้แต่ยังอยู่ในบิลด์
3. **schema drift** — ฐานจริงมี 22 ตารางกับราว 38 ฟังก์ชัน แต่ repo มี migration แค่สองไฟล์
   ควร `supabase db pull` เก็บของจริงเข้า repo ให้รีวิวได้ ของอย่าง `tms_carriers` ที่ว่างอยู่
   จะได้ไม่หลุดสายตาอีก

## แก้ UX หน้าผู้วางแผนงาน (18 สิงหาคม 2026)

ตรวจจากเว็บที่ deploy จริงด้วยบัญชีผู้วางแผนงาน แล้วแก้ห้าเรื่อง:

1. **ทางตันที่หน้าเที่ยวจาก TMS** — เที่ยวที่ TMS เพิ่งเปิด (สถานะ Confirm) ยังไม่มีชื่อคนขับ
   ปุ่มนำเข้าถูกปิดพร้อมข้อความ "ยังไม่จับคู่พนักงานขับ" แต่การ์ดปุ่มจับคู่ไม่ขึ้น
   เพราะการ์ดนั้นสร้างจากรายชื่อคนขับ ซึ่งตอนนั้นไม่มีสักชื่อ
   ตอนนี้แยก "รอ TMS จ่ายพนักงานขับ" ออกจาก "รอเราจับคู่" และย้ายปุ่มจับคู่ไปไว้ในแถวของเที่ยวนั้น
2. **ตัวเลขบนกระดานหน้าดึงข้อมูล** — นับเฉพาะวันที่แสดง แต่ป้ายเขียนเหมือนเป็นยอดรวมทั้งระบบ
   ระหว่างดึงข้อมูลจึงอ่านได้เป็น "0 เที่ยว 122 คัน" ตอนนี้บอกขอบเขตไว้บรรทัดเดียวเหนือแถวตัวเลข
3. **รูปแบบวันที่** — หน้าดึงข้อมูลเคยแสดง ISO ดิบปนกับ พ.ศ. ตอนนี้ผ่าน `fmtDate`/`fmtDateTime`
   ตัวกลางทั้งหมด รวมถึงหน้าผู้ใช้และหน้ากลุ่มสิทธิ์ที่ยังเรียก `toLocaleString` เอง
4. **เลขเที่ยวซ้ำ** — TMS ใช้เลขเที่ยวซ้ำข้ามคลังได้ สองแถวเลขเดียวกันดูเหมือนดึงซ้ำ
   ย้ายทะเบียนรถขึ้นไปอยู่คู่เลขเที่ยว เพราะทะเบียนคือตัวที่แยกออกจริง
5. **วันเริ่มต้นของหน้าเที่ยว** — เดิมเปิดมาที่วันล่าสุดที่มีข้อมูล ซึ่งมักเป็นวันที่กดอะไรไม่ได้เลย
   ตอนนี้เปิดที่วันล่าสุดที่ยังมีเที่ยวที่ทำอะไรได้จริง (migration `20260818010000`)

ยังไม่ได้ทำ (ข้อเสนอที่เหลือจากการตรวจ): แถบลำดับงาน 3 ขั้นข้ามหน้า, ปุ่มลัดใน empty state,
เปลี่ยนชื่อปุ่มดึงข้อมูลสองปุ่มให้ระบุช่วงวัน, ป้ายเตือนเมื่อข้อมูลเก่าเกิน 30 นาที

## ข้อมูล Trip ที่ดึงมาจริง (18 สิงหาคม 2026)

หนึ่งเที่ยวมี 20 ฟิลด์: `id` (GUID), `tripNo`, `warehouseId`, `warehouse`, `orderDate`,
`onDeliveryDate`, `licensePlate`, `driver`, `vehicleTypeName`, `carrierId`, `carrierName`,
`area`, `cost`, `actualCost`, `statusId`, `status`, `reason`, `totalPL`, `totalUnit`, `pickingLists[]`

สิ่งที่เห็นจากข้อมูลจริง:

- `cost` เป็น 1500 เท่ากันทุกเที่ยว (ค่าเหมา) ส่วน `actualCost` เป็นตัวที่ขยับจริง 0 / 1500 / 1900 / 2300
  โดย `0` แปลว่ายังไม่ปิดยอด ไม่ใช่ไม่มีค่าใช้จ่าย
- สถานะไล่จาก `Confirm` (2) ไป `Handling` (3) `OnDelivery` (4) `Completed` (5) `Cancelled` (6)
  **`Confirm` คือยังไม่ออกรถ ไม่ใช่จบงาน** เที่ยวที่ยังเป็น Confirm มักไม่มีทะเบียนและคนขับ
  เพราะ TMS จ่ายทีหลัง
- ใบในเที่ยวมีสองตระกูล: `PLVSP26xxxxxxxx` คือใบขายร้านค้า และ `TF-dd-mm-yyyy-n` คือโอนย้ายภายใน
  `pl_type` ที่พบคือ `SSP`, `SSA`, `TWT`, `NEW`, `TF`
- `tms_shipments.license_plate` และ `driver_name` เป็น null ทั้ง 1,682 แถว — เส้น Picking List
  ไม่ส่งสองค่านี้มา มีแต่เส้น Trip ที่มี

## ดึงทุกคลังทุกสถานะ + แยกชื่อคนขับ (18 สิงหาคม 2026)

- เคยลองเปิด `ALLOWED_WAREHOUSES` เป็นทุกคลังแล้ว **ถอยกลับเป็น `KM23-CW-01`, `KM23-CW-02` ตามเดิม**
  บัญชีคนวางแผนเห็น 6 คลัง ซึ่งรวมของแผนกอื่นที่ไม่ใช่งานของระบบนี้
  ด่าน carrier กันผู้รับจ้างรายอื่นได้ แต่กันงานของกองรถเราที่คลังอื่นดูแลเองไม่ได้
  ขอบเขตคลังจึงเป็นเรื่องที่คนกำหนด ไม่ใช่เดาจากสิทธิ์ของบัญชี TMS
- สถานะไม่เคยถูกกรองบนเส้น Trip อยู่แล้ว — request ส่งแค่ paging กับ ordering
- เที่ยวไม่มีรหัสคลังติดมาใน payload ทุกแถวจึงเคยมี `warehouse_code` เป็น null
  ตอนนี้ประทับรหัสคลังที่ยิงถามลงไปเอง
- ชื่อคนขับจาก TMS เป็นข้อความก้อนเดียว เที่ยวที่ไปสองคนมาเป็น
  `"เอกชัย บุญอินทร์ (เอก) , อณัฐ อาดัม (นัท)"` และบางแถวมีคอมมาห้อยท้าย
  เพิ่ม `app.tms_driver_names()` แยกชื่อ แล้วให้ `import_tms_trip` บังคับจับคู่ครบทุกชื่อ
  คนแรกเป็นคนขับของเที่ยว คนที่เหลือบันทึกในหมายเหตุ และทุกคนถูกตั้งสถานะ `on_trip`
  หน้าจอแสดงปุ่มจับคู่หนึ่งปุ่มต่อหนึ่งคน (migration `20260818020000`)

## บั๊กนำเข้าเที่ยวล้ม (18 สิงหาคม 2026)

`null value in column "origin" of relation "orders" violates not-null constraint`

`import_tms_trip` อ่านชื่อต้นทางจาก `settings.org_name` ด้วย `select ... into` ซึ่งไม่คืนแถวเลย
เมื่อยังไม่เคยตั้งคีย์นั้น (ตาราง `settings` ว่างอยู่จริง) ตัวแปรจึงเป็น null ทั้งที่เขียน `coalesce` ไว้แล้ว
เพราะ `coalesce` กันได้แค่ "มีแถวแต่ค่าว่าง" ไม่ใช่ "ไม่มีแถว" แก้ให้ fallback เป็น `คลังบริษัท`
(migration `20260818030000`)

## รอบดึงอัตโนมัติ = วันนี้ (18 สิงหาคม 2026)

- `pullRecentTrips` เคยกวาด -3 ถึง +14 วันทุก 5 นาที ตอนนี้เหลือวันนี้วันเดียว
  ช่วงกว้างยังดึงได้จากช่องเลือกวันที่ในหน้าเดียวกัน
- `preview_tms_trips` เลือกวันเริ่มต้นตามลำดับ: วันนี้ (ถ้ามีเที่ยวที่ทำได้) >
  วันล่าสุดที่ยังมีเที่ยวทำได้ > วันล่าสุดที่มีข้อมูล (migration `20260818040000`)
- **เที่ยวสถานะ Completed ยังนำเข้าได้โดยตั้งใจ** และไม่ได้สร้างงานเปิดค้าง —
  `import_tms_trip` มี `if v_status <> 'completed'` กันไม่ให้จับรถ/คนขับเป็น `on_trip`
  การนำเข้าเที่ยวที่จบแล้วคือการบันทึกประวัติ ถ้าบล็อกไปเลย รายงานจะขาดทุกเที่ยวที่จบ
  ก่อนคนกดนำเข้าทัน ซึ่งเกิดบ่อยเพราะรอบดึงห่าง 5 นาที

## ลบพนักงานขับ + ชื่อซ้ำ (18 สิงหาคม 2026)

- นโยบาย RLS `drivers_delete` อ้าง `app.has_perm('drivers.delete')` แต่สิทธิ์ชื่อนี้
  **ไม่เคยถูกใส่ใน `permissions` และไม่เคยแจกให้บทบาทไหนเลย** ปุ่มลบผูกกับคีย์เดียวกัน
  จึงไม่โผล่ให้ใครเห็นรวมทั้ง admin — ขึ้นทะเบียนแล้วแจกให้ `admin` กับ `dispatcher`
- `create_driver_from_tms` หาคนซ้ำจาก `tms_driver_map` ทางเดียว ตอนยังไม่แยกชื่อ
  คีย์คือชื่อควบสองคน พอแยกชื่อแล้วได้คีย์ใหม่ มองไม่เห็นของเดิม เลยสร้างซ้ำ
  ตอนนี้ถ้าคีย์ยังไม่เคยจับคู่ จะหาต่อจากชื่อที่ตัดช่องว่างแล้วตรงกันก่อนสร้างใหม่
- เพิ่ม RPC `delete_driver` ให้ได้ข้อความไทยแทน error ของ FK และเก็บกวาดคีย์ใน
  `tms_driver_map` ไปด้วย (migration `20260818050000`)

## ค่าขนส่ง เลข PL และรหัสสินค้า (18 สิงหาคม 2026)

ทั้งสามอย่างถูกดูดมาครบตั้งแต่แรก แต่หายไปคนละจุด:

- **ค่าขนส่ง** — `tms_trips.cost` / `actual_cost` มีค่าอยู่ แต่ `preview_tms_trips`
  ไม่เคย select ออกมา และ `trips` ไม่มีช่องให้ลง (มีแต่ `fuel_cost` / `toll_cost` / `other_cost`
  ซึ่งเป็นต้นทุนที่เราจ่ายเองระหว่างทาง คนละก้อนกัน)
  เพิ่ม `trips.freight_cost` / `freight_actual_cost` แล้วให้ `import_tms_trip` คัดลอกมา
- **เลข PL** — เขียนลง `orders.tms_picking_list_no` ตั้งแต่นำเข้าแล้ว แต่ไม่มีหน้าไหนแสดง
  ตอนนี้ขึ้นในหน้าเที่ยว (สูงสุด 6 ใบ เกินนั้นเป็น `+n`) และในหน้าออเดอร์
- **รหัสสินค้า** — `import_tms_trip` ยุบของทั้งใบด้วย
  `string_agg(distinct coalesce(item_name, item_no))` ซึ่ง `coalesce` ใช้ชื่อก่อนเสมอ
  รหัสจึงเหลือแค่ 8 แถวจาก 1,708 ที่ไม่มีชื่อ และจำนวนต่อรายการหายหมด
  เพิ่มตาราง `order_items` (`order_id`, `item_no`, `item_name`, `qty`) พร้อม unique index
  กันยอดบวมตอนนำเข้าซ้ำ

**0 ของ TMS แปลว่า "ยังไม่ลงตัวเลข" ไม่ใช่ "ฟรี"** ทุกจุดจึงแปลงเป็น null ก่อนเก็บ/ก่อนส่งออก
ไม่งั้นยอดรวมในรายงานจะถูกดึงลงด้วยเที่ยวที่ยังไม่ปิดยอด

ของที่นำเข้าไปก่อนหน้านี้ถูกเติมย้อนหลังแล้ว (migration `20260818100000`) — ตรวจแล้วได้
รายการของ 41 แถว และค่าขนส่ง 8 จาก 9 เที่ยว (อีกเที่ยวเป็นงานที่สร้างเองในระบบ ไม่มีตัวเลขจาก TMS)

## นำเข้าเที่ยว Completed อัตโนมัติ (18 สิงหาคม 2026)

การนำเข้าอัตโนมัติเดิมทำในเบราว์เซอร์ โดยเรียก `previewTrips()` แบบไม่ส่งวันที่
ซึ่งคืนข้อมูล **วันเดียว** รอบอัตโนมัติจึงเห็นแค่วันเริ่มต้นของหน้า พอกฎวันเริ่มต้น
ถูกเปลี่ยนให้เอาวันนี้ก่อน เที่ยวที่ปิดงานไปเมื่อวานก็ไม่มีใครเก็บอีกเลย

ย้ายมาเป็น RPC `auto_import_trips()` กวาดได้ทุกวันในคราวเดียว ไม่ผูกกับวันที่หน้าจอเปิดอยู่:

- **Completed (5)** — เก็บย้อนหลังทุกวัน งานจบแล้ว นำเข้า = บันทึกประวัติ
  และ `import_tms_trip` ไม่จับรถ/คนขับเป็น `on_trip` อยู่แล้วเมื่อสถานะเป็น `completed`
- **สถานะอื่น** — เฉพาะวันนี้ การสร้างงานที่ยังไม่จบของเมื่อวานขึ้นมาใหม่
  เท่ากับปลุกงานค้างที่คนอาจจัดการนอกระบบไปแล้ว
- ต้องจับคู่ชื่อคนขับ **ครบทุกคน** ก่อนเสมอ เทียบจำนวนชื่อกับจำนวนที่จับคู่ได้
  ไม่ใช่เช็คแค่ชื่อแรก เที่ยวที่ยังไม่ครบถูกข้ามไว้ให้คนกดเอง
- เที่ยวที่นำเข้าไม่ผ่านไม่หยุดทั้งรอบ นับเป็น `failed` แล้วขึ้นบนหน้าดึงข้อมูล

(migration `20260818110000`)
